# Дальнейший план работы — после T0/T1 (2026-07-01)

Контекст: завершены и **задеплоены в прод** (`main` → CI → VPS, домен contento-ai.ru):
- `fix(ai)` 428ffbf — онбординг-анализ стал актуальным (дата в промпт + live web_search; фолбэк если поиск недоступен).
- `feat(web)` 7ae3730 — **T0** (дизайн-плашки уведомлений вместо сырых ошибок, RU/EN) + **T1** (локализация типов целей в Бренд-ките).
- Ранее: `feat(t5)` 032f4fe — обязательный онбординг + авто-бренд-кит.

Проверки на этой итерации: web typecheck/lint/build ✅, en/ru паритет 839/839 ✅, api-тесты 29/29 ✅, прод healthy (api `{"status":"ok"}`, web 200).

---

## 1. Диагностика (что нашёл, помимо выполненного)

| # | Находка | Severity | Действие |
|---|---|---|---|
| D1 | **Визуальный QA T0/T1 не сделан** — плашки и лейблы целей собраны и задеплоены, но глазами в браузере (с логином) не проверены. curl упирается в редирект на sign-in. | M | Залогиниться → `/ru/brand` (вкладка Цели) + спровоцировать ошибку (напр. отключить сеть) → увидеть плашку. Через `/qa` или вручную. |
| D2 | **`activity-logger.ts:75` кидает `Cannot read properties of undefined (reading 'create')`** в api-тестах (onResponse-хук пишет ActivityLog в неинициализированную в тестах prisma). Тесты зелёные (29/29), ошибка только в логах. | Low (debt) | Гард `if (prisma.activityLog)` или мок в тест-сетапе. Не трогал — вне scope (CLAUDE.md). |
| D3 | **Латентность онбординга выросла:** портрет теперь ~40с (web_search) + бренд-кит (~vt sonnet) = ~50–70с суммарно на один проход визарда. | M | Приемлемо для разового действия со спиннером. Опц.: снизить `web_search max_uses` 3→2; параллелить портрет/кит нельзя (кит зависит от портрета). Следить за прокси-таймаутами Caddy (сейчас ок). |
| D4 | **T0 покрывает только `ErrorBanner`** (17 мест). Если где-то ошибки рендерятся «сырым» `<div>` мимо ErrorBanner — не апгрейдились. | Low | Грепнуть на прямые `text-red-700` рендеры строк ошибок; перевести на `<SystemNotice>`. |

---

## 2. Остаток UX-плана (2026-06-30) — приоритет «быстрые победы»

### T2 — Прояснить «Превью голоса бренда» (S, copy-only)
- Кнопка `brand/page.tsx:235` = `t('previewVoice')` = «Превью голоса бренда» — непонятно. Бэкенд (`/brand-preview`) **рабочий**, генерит 3 примера.
- Переименовать → «**Сгенерировать пример в голосе бренда**»; переписать заголовок/описание модалки; добавить inline-подсказку; подписать 3 примера их ракурсами (личность/ценности/связь с ЦА); подсказка при пустом бренде.
- Файлы: `brand/page.tsx`, `messages/{en,ru}.json`. Без бэкенда.

### T3 — Гейт создания кампании по портрету (S, defense-in-depth)
- **Подтверждено диагностикой:** reject летит поздно — `campaigns.ts:204` (на `content-plan/generate`), а `POST /campaigns` не проверяет портрет.
- **API:** в `POST /campaigns` перед create — `findUnique companyPortrait`; нет → 400 (+ в response-схему). **UI:** `studio/page.tsx` + `campaigns/new` — `GET /company-portrait`; null → дизейбл кнопок + плашка T0 с действием «Перейти к онбордингу». Тест в `campaigns.test.ts`.
- Теперь, когда онбординг обязателен (T5), это в основном страховка — но закрывает гонку «портрет ещё генерится».

### T4 — Починить Тренды (S config / M seed+UI)
- **Подтверждено:** ничто не сеет `TrendFeedConfig` (греп пустой) → воркер читает 0 строк → 0 трендов.
- **Быстро:** через OWNER-роут `POST /workspaces/{id}/trend-feed-configs` посеять RSS (RU) + reddit без ключей. Проверить, что `trend-fetcher`/`trend-analyzer`/redis/kafka подняты на проде.
- **Out-of-box:** Prisma-seed/INSERT дефолтных key-free источников, чтобы свежая установка работала сразу. UI: связать существующую страницу Settings → Trend Sources с `trend-feed-configs`.

**Рекомендуемый порядок спринта:** T2 (5 мин copy) → T3 (API-гейт + UI + тест) → T4 (seed + проверка воркеров). Один деплой на все три.

---

## 3. Launch-readiness (из стратегического аудита, P0/P1) — нужен доступ к серверу/внешние сроки

- **OPS-1** (P0, нужен ты): проверить `infra/.env` на проде — не дефолтные пароли PG/MinIO, сильный `JWT_SECRET`, заполнены ключи (Anthropic/OpenAI/Higgsfield/ElevenLabs/YouTube). Из репо не верифицируется.
- **OPS-2** (P0): подтвердить video-worker на проде — реальный stitch (можно `HIGGSFIELD_MOCK=1`) → mp4; профиль `video` поднят.
- **OPS-3** (P0): медиа-доступность — внешне резолвящийся presign-URL (S3/CDN, не localhost MinIO), иначе паблиш видео упадёт.
- **WEDGE-1** (P1, внешний срок недели): подать заявки Meta IG Business + TikTok audited app на review **сейчас** — фидбэк-петля слепа вне YouTube без них.
- **i18n-remainder** (P1): после T0/T1 локализованы плашки + цели; остаётся доделать RU/EN на крупных экранах (`brand` частично, `analytics`, `calendar`, settings-подстраницы и т.д.).
- **AUTH-HARDEN** (P1 для публичного запуска): password-reset + CSP/CSRF (cookie сейчас non-httpOnly).

---

## 4. Что я предлагаю делать следующим

1. **Сейчас (S, без тебя):** T2 → T3 → T4 одним спринтом + один деплой. Закрывает остаток UX-плана.
2. **Визуальный QA (D1):** после деплоя — прогнать `/ru/brand` и онбординг с логином, заскринить плашки/лейблы.
3. **Тебе (внешнее/доступ):** OPS-1/2/3 (аудит прод-сервера) + подать app-review Meta/TikTok (WEDGE-1) — тикают недели.
4. **Долг (по желанию):** D2 (activity-logger гард), D3 (max_uses 3→2), D4 (грепнуть сырые ошибки), `currentDateContext()` в idea-generator/scriptwriter.

Жду твоего ревью этого плана — с какого пункта начинать.
